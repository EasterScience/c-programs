#include<stdio.h>
#include<conio.h>
void main()
{
        char source[20]="EASTERSCIENCE";
        char destination[20];
        strcpy(destination, source);
        printf("Value of second string is: %s", destination);
        getch();
}
